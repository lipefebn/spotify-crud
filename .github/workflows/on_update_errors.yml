name: On update errors 

on:
  push:
    branches:
      - 'main'
    tags-ignore:
      - '*.*'

env:
  TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PATH: 'src/adapter/query'

jobs:
  update_wiki:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Render PUML to wiki
        uses: lipefebn/env@v0.2

